
// Funcion para calcular la nota media de cada alumno:

// La función "calcularMedia" recibe como parámetro un puntero a una estructura de tipo "alumno" y calcula la nota
// media de las asignaturas de ese alumno, guardando el resultado en el campo "media" de la estructura.

void calcularMedia(struct alumno *alumno) {
    float suma = 0;
    for (int i = 0; i < 5; i++) {
        suma += alumno->asignaturas[i];
    }
    alumno->media = suma / 5;
}
